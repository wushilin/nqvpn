[listen]
api = "127.0.0.1:18443"     # HTTPS: /api/v1/* and /ui (self-signed unless [tls] is set)
quic = "0.0.0.0:14433"      # QUIC control plane; published to members at join

[state]
dir = "./state"

[admin]
bearer_token = "dev-admin-token"
